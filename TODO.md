# TODOLIST

- use same form to create and modify an entity : 
    - rename the file `create` as `form`
    - in controller , in method create and method modify , the `render()` method must call the view `form...php`
- in dashboard > every widgets > when mouse is over a line (of bankAccount or thirdParty , ..) add a css 'hover' background color.
- in dashboard > every widgets > add button to modify and delete an entity.


